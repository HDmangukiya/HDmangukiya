name: Sync Profile README

on:
  push:
    paths:
      - README.md

jobs:
  sync-readme:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout HardikMangukiya repo
      - name: Checkout HardikMangukiya
        uses: actions/checkout@v3

      # Step 2: Clone your profile repository
      - name: Clone profile repo
        run: |
          git clone https://github.com/HDmangukiya/HardikMangukiya.git profile
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Step 3: Copy README.md
      - name: Copy README to profile repo
        run: |
          cp README.md profile/README.md

      # Step 4: Commit and push changes
      - name: Commit and push
        run: |
          cd profile
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Update profile README" || echo "No changes to commit"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
